# PgBouncer Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# Production Database (Self-hosted Supabase)
DATABASE_URL=postgres://postgres:your_password@data.greenland77.ge:5432/postgres

# Alternative: Separate variables
DB_HOST=data.greenland77.ge
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password

# Development Database (for testing)
DEV_DATABASE_URL=postgres://postgres:dev_password@db.akxmacfsltzhbnunoepb.supabase.co:5432/postgres

# PgBouncer Settings (optional overrides, defaults in pgbouncer.ini)
POOL_MODE=transaction
DEFAULT_POOL_SIZE=20
MAX_CLIENT_CONN=100
RESERVE_POOL_SIZE=5
