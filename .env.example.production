# Production Environment Variables for Dockploy Deployment
# Copy these variables to your Dockploy project settings under "Environment Variables"

# ===================================
# REQUIRED - Application will NOT work without these
# ===================================

# Supabase Configuration
# Your production Supabase instance URL
NEXT_PUBLIC_SUPABASE_URL=https://data.greenland77.ge

# Supabase Anonymous Key (Public key - safe to expose to clients)
# Get this from: Supabase Dashboard → Project Settings → API → anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Application URL
# The public URL where your app will be accessible
NEXT_PUBLIC_APP_URL=https://greenland77.ge

# ===================================
# OPTIONAL - Monitoring & Analytics
# ===================================

# Sentry Error Tracking (Optional)
# If you're using Sentry for error monitoring
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
# SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# ===================================
# NOTES FOR DOCKPLOY CONFIGURATION
# ===================================

# 1. In Dockploy, go to your project → Settings → Environment Variables
# 2. Add each variable above (remove the # comments)
# 3. Make sure to use the actual values, not the placeholder text
# 4. Click "Save" and redeploy your application

# The following variables are automatically set by docker-compose.yml
# and do NOT need to be added in Dockploy:
# - NODE_ENV=production
# - NEXT_TELEMETRY_DISABLED=1
# - PORT=3000
# - HOSTNAME=0.0.0.0
