# ============================================================================
# Monitoring Stack Environment Variables
# ============================================================================
# Purpose: T040-T050 - Configuration for Docker Compose
# Instructions:
#   1. Copy this file to .env
#   2. Update all passwords with strong values
#   3. Never commit .env to git (already in .gitignore)
# ============================================================================

# ============================================================================
# PostgreSQL Monitoring User (created in migration 20251125000010)
# ============================================================================
# This is the password for the "monitoring" role in PostgreSQL
# Change from default: monitoring_change_this_password
MONITORING_PASSWORD=your_secure_monitoring_password_here

# ============================================================================
# Grafana Admin User
# ============================================================================
# Default admin username (can be changed)
GRAFANA_USER=admin

# Grafana admin password
# Default: admin (MUST CHANGE for production!)
GRAFANA_PASSWORD=your_secure_grafana_password_here

# ============================================================================
# Security Notes
# ============================================================================
# 1. Use strong passwords (16+ characters, mixed case, numbers, symbols)
# 2. Never use default passwords in production
# 3. Never commit .env to git
# 4. Rotate passwords regularly (every 90 days recommended)
# 5. Store passwords securely (password manager, secrets vault)
#
# Example strong password generation:
#   openssl rand -base64 32
#   pwgen -s 32 1
#
# ============================================================================
# Connection String Examples
# ============================================================================
# The monitoring password is used in docker-compose.yml:
#   DATA_SOURCE_NAME=postgresql://monitoring:${MONITORING_PASSWORD}@data.greenland77.ge:5432/postgres?sslmode=require
#
# To change the monitoring password in PostgreSQL:
#   psql -U postgres -c "ALTER ROLE monitoring WITH PASSWORD 'new_password';"
